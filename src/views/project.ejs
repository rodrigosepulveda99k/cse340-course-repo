<%- include('partials/header') %>

<main>
    <h1><%= project.title %></h1>

    <section class="project-detail">
        <p><strong>Description:</strong> <%= project.description %></p>
        <p><strong>Date:</strong> <%= project.date ? project.date.toISOString().split('T')[0] : 'TBD' %></p>
        <p><strong>Location:</strong> <%= project.location %></p>
        <p>
            <strong>Partner organization:</strong>
            <a href="/organization/<%= project.organization_id %>">
                <%= project.organization_name %>
            </a>
        </p>

        <% if (locals.user) { %>
            <div class="volunteer-actions" style="margin-top: 20px; padding: 15px; background: #f4f4f4; border-radius: 8px; border: 1px solid #ddd;">
                <% if (isVolunteering) { %>
                    <p style="color: #2e7d32;"><strong>✅ You are volunteering for this project!</strong></p>
                    <form method="POST" action="/volunteer/remove/<%= project.project_id %>" style="display:inline;">
                        <button type="submit" class="btn-delete" style="color: #d32f2f; background: none; border: none; cursor: pointer; text-decoration: underline; padding: 0;">
                            Remove me as Volunteer
                        </button>
                    </form>
                <% } else { %>
                    <p>Help this cause:</p>
                    <form method="POST" action="/volunteer/add/<%= project.project_id %>">
                        <button type="submit" class="btn-primary" style="background: #007bff; color: white; padding: 10px 15px; border-radius: 5px; border: none; cursor: pointer;">
                            Volunteer for this project
                        </button>
                    </form>
                <% } %>
            </div>
        <% } else { %>
            <div style="margin-top: 20px; padding: 10px; border: 1px dashed #ccc;">
                <p>Please <a href="/login">login</a> to join this project as a volunteer.</p>
            </div>
        <% } %>
        
        <hr style="margin: 30px 0;">

        <div class="project-categories">
            <% if (locals.categories && categories.length > 0) { %>
                <h2>Categories</h2>
                <ul class="tags-list">
                    <% categories.forEach(category => { %>
                        <li>
                            <a href="/category/<%= category.category_id %>" class="category-tag">
                                <%= category.category_name %>
                            </a>
                        </li>
                    <% }); %>
                </ul>
            <% } %>
            
            <% if (isLoggedIn && user && user.role_name === 'Admin') { %>
            <p class="action-link">
                <a href="/assign-categories/<%= project.project_id %>" class="btn-edit">
                    <%= (locals.categories && categories.length > 0) ? 'Edit Categories' : 'Add Categories to Project' %>
                </a>
            </p>
            <% } %>
        </div>
    </section>
</main>

<%- include('partials/footer') %>